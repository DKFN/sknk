import server from "./server/sknkServer";

// TODO : Complete
/*
export class LayoutSpace {
    public constructor() {
        super();
        const maybeName: string = this.getAttribute("space-name");
        const spaceId = server.registerSpace({name: maybeName});
        this.setAttribute("skunk-id", spaceId);
    }
}

class Layouter {
}
*/
